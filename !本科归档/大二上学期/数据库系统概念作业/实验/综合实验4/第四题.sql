SELECT
    USER.NICKNAME,
    MESSAGE.CONTENT,
    SEND.SENT_TIME
FROM
    SEND
    JOIN MESSAGE
    ON SEND.MID = MESSAGE.MID
    JOIN USER
    ON SEND.UID_SENDER = USER.UID
WHERE
    SEND.UID_RECEIVER = 1
    AND STATUS = "待发送"
ORDER BY
    USER.NICKNAME,
    SEND.SENT_TIME DESC;