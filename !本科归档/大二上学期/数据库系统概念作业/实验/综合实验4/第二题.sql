SELECT
    USER1_FRIEND.NICKNAME,
    MOMENT.CONTENT,
    MOMENT.POST_TIME
FROM
    USER1_FRIEND
    JOIN MOMENT
    ON MOMENT.UID = USER1_FRIEND.UID
WHERE
    MOMENT.TYPE = "公开"
    OR MOMENT.TYPE = "仅好友可见"
ORDER BY
    MOMENT.POST_TIME DESC;