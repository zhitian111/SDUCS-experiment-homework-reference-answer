SELECT
    VIDEO.CHANEL,
    COUNT(*) AS SUM
FROM
    VIDEO
    JOIN WATCH
    ON WATCH.VID = VIDEO.VID
    JOIN [USER]
    ON [USER].UID = WATCH.UID
WHERE
    [USER].UID = 15
GROUP BY
    CHANEL
ORDER BY
    SUM DESC LIMIT 5;
