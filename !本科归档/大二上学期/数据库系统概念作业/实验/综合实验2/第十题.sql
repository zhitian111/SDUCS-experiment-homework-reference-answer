SELECT
    VIDEO.NAME,
    VIDEO.DESCRIPTION,
    POST.POST_TIME
FROM
    VIDEO
    JOIN POST
    ON POST.VID = VIDEO.VID
    JOIN FOLLOW
    ON FOLLOW.UID_FOLLOWED = POST.UID
WHERE
    FOLLOW.UID_FOLLOWER = 15
ORDER BY
    POST.POST_TIME DESC;