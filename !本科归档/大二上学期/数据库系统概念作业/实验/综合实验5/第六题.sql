SELECT
    TEAM.NAME,
    COUNT(*) AS CNT
FROM
    KILL
    JOIN PLAYER
    ON PLAYER.PID = KILL.PID_KILLER
    JOIN TEAM
    ON PLAYER.TID = TEAM.TID
GROUP BY
    PLAYER.TID
ORDER BY
    COUNT(*) DESC LIMIT 5;